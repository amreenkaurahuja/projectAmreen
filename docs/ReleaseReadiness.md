# Release Readiness

A one-page gate, filled in before every release is tagged. See `docs/Roadmap.md` → "Process" (Rule 13): if any category below isn't ✅, that's what gets worked on next — not new features.

## Template

| Category      | Status       | Evidence | If not ✅ |
| ------------- | ------------ | -------- | --------- |
| Architecture  | ✅ / ⚠️ / ❌ | —        | —         |
| Database      | ✅ / ⚠️ / ❌ | —        | —         |
| Security      | ✅ / ⚠️ / ❌ | —        | —         |
| Performance   | ✅ / ⚠️ / ❌ | —        | —         |
| AI            | ✅ / ⚠️ / ❌ | —        | —         |
| Testing       | ✅ / ⚠️ / ❌ | —        | —         |
| Documentation | ✅ / ⚠️ / ❌ | —        | —         |
| Operations    | ✅ / ⚠️ / ❌ | —        | —         |

`✅` complete, no known gaps. `⚠️` known, accepted/documented gap — not blocking this release. `❌` blocking — do not tag until resolved.

---

## Release 0.7 — AI Learning Companion (Question Explainer) & Learning Metrics

Filled in from the Stage 7.1 Operational Readiness Audit (full ten-section audit — feature flags, environment, migrations, deployment, logging, failure matrix, rollback, dependencies, operational limitations — in that review) plus Stage 6.5's end-to-end behavioural verification of the learning metrics pipeline.

| Category      | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                       | If not ✅                                                                                                                                                     |
| ------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Architecture  | ✅     | TDS-008's full architecture (publisher boundary, append-only persistence, event identity, delivery/idempotency, reporting contract) verified with zero contradiction by Stage 6.5's end-to-end tests against the real pipeline. Question Explainer reuses TDS-007's already-shipped, already-audited AI Platform integration unchanged.                                                        | —                                                                                                                                                             |
| Database      | ✅     | Migrations `0011`–`0013` reviewed for sequential numbering, FK/RLS/index ordering, and irreversible operations (Stage 7.1 §3) — a brand-new database migrates to Release 0.7 with no manual intervention. `learning_events`' RLS is select+insert only (no update/delete), enforcing immutability structurally.                                                                                | —                                                                                                                                                             |
| Security      | ✅     | `learnerId` never client-supplied anywhere in either capability (resolved server-side from `attemptId`, same pattern both times); `event_id`/`session_id`/`requestId` kept structurally distinct; logging reviewed and confirmed identifier-only, no educational content, no prompts (Stage 7.1 §5).                                                                                           | —                                                                                                                                                             |
| Performance   | ⚠️     | No performance targets were documented for either capability this release (unlike v0.6.0's AI Platform, which has documented targets in `AI_PLATFORM.md`) — neither Question Explainer generation latency nor metrics delivery throughput was assessed against a target.                                                                                                                       | Document targets if/when either capability sees real traffic volume that makes latency a concern — not before.                                                |
| AI            | ✅     | Question Explainer's AI path is the AI Platform's existing gateway/budget/health/grounding-validator, unchanged (TDS-007 §0: "no architectural changes expected"). Every failure mode (disabled, no key, timeout, quota, malformed JSON, failed grounding, budget exhausted) degrades to a deterministic fallback, matching Rule 14.                                                           | —                                                                                                                                                             |
| Testing       | ⚠️     | 794 unit/component tests passing (91 files) plus full behavioural traceability (`docs/testing/learning-metrics-behaviour.md`). Same inherited gap as v0.6.0: e2e has never run against a real environment — this app's only Supabase project is production (no dedicated staging project exists), so real-environment e2e was not attempted for either capability, by decision, not oversight. | Same as v0.6.0's open item: create a dedicated non-production Supabase project, then run e2e against it. Until then, this row stays ⚠️ by design.             |
| Documentation | ✅     | `ENVIRONMENTS.md`, `.env.example`, `Database.md`, `CHANGELOG.md`, and this row all brought current as part of Stage 7.2 (the `AI_QUESTION_EXPLAINER_ENABLED` gap and the obsolete `seed:questions` reference Stage 7.1 found were both fixed in the same change that added this row).                                                                                                          | —                                                                                                                                                             |
| Operations    | ⚠️     | Stage 7.1's primary finding: migration application is fully manual and decoupled from Vercel's automatic deploy-on-merge — see `docs/releases/release-0.7-rollout.md`. Inherited, still open: in-memory/per-process budget guard (v0.6.0), no dedicated preview/staging Supabase project, no live Sentry project configured. None of these are learner-impacting or data-integrity risks.      | Confirm migrations `0011`–`0013` are applied to the live Supabase project before/alongside this release (operational step, cannot be verified from the repo). |

**Release decision**: Ready, conditional on confirming (outside this repository) that migrations `0011`–`0013` have been applied to the live Supabase project. The two ⚠️ rows are explicitly accepted, inherited-or-documented gaps — not oversights — consistent with how v0.6.0 handled the same testing-environment limitation.

---

## Release 0.6 — Learning Intelligence Platform (AI Learning Coach)

Filled in from the SDS-002 completion audit (full requirement-by-requirement matrix in that review) plus the Sprint 1.5 hardening pass that followed it.

| Category      | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | If not ✅                                                                                                                                                                                         |
| ------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Architecture  | ✅     | Dependency rules documented and grep-verified (`Architecture.md` → "Dependency rules": nothing outside `src/modules/ai/` imports from it); ADR-004 records the `platform/` vs `modules/` decision. Zero blocking findings in SDS-002.                                                                                                                                                                                                                                                                                                | —                                                                                                                                                                                                 |
| Database      | ✅     | Migration `0010_phase5_ai_learning_coach.sql` — indexes, RLS matching the existing "Parents manage own X" pattern, `ON CONFLICT`-safe concurrency (3 dedicated tests).                                                                                                                                                                                                                                                                                                                                                               | —                                                                                                                                                                                                 |
| Security      | ✅     | DTO boundary (no PII/UUIDs/answers cross into prompts), ownership enforced transitively, logging allow-listed by type, prompt-injection mitigation now covered by a regression test (Sprint 1.5, `ai-prompts.test.ts` → "prompt injection resistance").                                                                                                                                                                                                                                                                              | —                                                                                                                                                                                                 |
| Performance   | ✅     | Targets documented (`AI_PLATFORM.md` → "Performance Targets"); deliberately not enforced by automated timing assertions (would be flaky by nature) — a documented choice, not a gap.                                                                                                                                                                                                                                                                                                                                                 | —                                                                                                                                                                                                 |
| AI            | ✅     | Provider abstraction, Gemini implementation, timeout (verified with a real fake-timer-elapse test, not just a mocked error), quota handling, grounding, deterministic fallback all Complete per SDS-002.                                                                                                                                                                                                                                                                                                                             | —                                                                                                                                                                                                 |
| Testing       | ⚠️     | 532 unit/component tests passing (63 files). `tests/e2e/ai-coach.spec.ts` exists and is well-formed (confirmed: runs and skips cleanly without credentials), but **has never executed against a real environment**. The only Supabase project this app has is `main` / **PRODUCTION** (`docs/ENVIRONMENTS.md` already flags: no dedicated preview/staging project exists) — running E2E there would mutate real data under a real account, so it was explicitly **not** run against it for this release, by decision, not oversight. | Create a dedicated non-production Supabase project (closes a gap `docs/ENVIRONMENTS.md` already tracked before this release), then run E2E against that. Until then, this row stays ⚠️ by design. |
| Documentation | ✅     | `AI_PLATFORM.md`, `AI_CONSTITUTION.md`, `Architecture.md`, `ENVIRONMENTS.md`, `.env.example`, `CHANGELOG.md` all current. The one inaccuracy SDS-002 found (versioning claim overstating centralization) fixed in Sprint 1.5.                                                                                                                                                                                                                                                                                                        | —                                                                                                                                                                                                 |
| Operations    | ⚠️     | Budget guard (`BudgetManager`) and health tracker (`ProviderHealthTracker`) are both in-memory/per-process by design — accurate within one warm instance, don't aggregate across instances, reset on cold start. Estimated spend is computable (`getSnapshot()`) but not yet logged or exposed anywhere. All explicitly accepted as out of scope for 0.6 (no Redis, no persistent budget manager, no ops dashboard — see `AI_PLATFORM.md` → "Observability").                                                                        | Revisit if/when real traffic makes the in-memory approximation inadequate — not before.                                                                                                           |

**Release decision**: Ready now. Both ⚠️ rows are explicitly accepted, documented gaps — not oversights, not blockers — decided as part of this release rather than discovered after it.
